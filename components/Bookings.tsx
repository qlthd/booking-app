import * as React from 'react'
import { Text, SafeAreaView } from 'react-native';

export const Bookings = () => {
 
  return (
    <SafeAreaView>
      <Text>Bookings</Text>
    </SafeAreaView>
  );
}

